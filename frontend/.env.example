# ╔══════════════════════════════════════════════════════════════╗
# ║        StudyMate AI — Frontend Environment Variables         ║
# ║  Copy this file → rename to ".env" → fill in your values   ║
# ╚══════════════════════════════════════════════════════════════╝
#
# WHERE TO FIND THESE VALUES:
#   1. Go to https://supabase.com/dashboard
#   2. Open your project
#   3. Go to Settings → API
#   4. Copy the values below from there
#
# IMPORTANT:
#   - This file (.env.example) is safe to commit to git ✅
#   - The real .env file is gitignored — NEVER commit it ❌
#   - VITE_ prefix = safe for browser (public keys only)
#   - Service role key = NEVER put it in the frontend

# ─── Supabase Project ────────────────────────────────────────────
# Found at: Dashboard → Settings → API → "Project URL"
VITE_SUPABASE_URL=https://rfeirfwtlmlyebfqmnen.supabase.co

# Found at: Dashboard → Settings → API → "Project API keys" → anon / public
VITE_SUPABASE_ANON_KEY=your-eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJmZWlyZnd0bG1seWViZnFtbmVuIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODQzODkzOTQsImV4cCI6MjA5OTk2NTM5NH0.Nj_0WL4x6xksgyfoJ4nnMoI3WsB-fef-uywVRDJ-Cdg

# ─── AI Provider (used only in Edge Functions, NOT here) ─────────
# These go into Supabase Edge Function secrets, not in this file.
# See: Dashboard → Edge Functions → Secrets
# GEMINI_API_KEY=your-gemini-api-key        ← add in Supabase dashboard
# OPENAI_API_KEY=your-openai-api-key        ← add in Supabase dashboard (if using OpenAI)
