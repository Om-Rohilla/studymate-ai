# For local development via supabase start
project_id = "rfeirfwtlmlyebfqmnen"

[api]
enabled = true
port = 54321

[auth]
enabled = true
site_url = "http://127.0.0.1:5173"

[functions.ai-service]
# AI generation is a paid, authenticated feature. Reject anonymous invocations
# at the platform layer before the function reaches an AI provider.
verify_jwt = true
